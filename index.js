export const metadata = Object.freeze({
  name: "zig-node",
  version: "0.0.1",
  status: "placeholder"
});

export function describe() {
  return "zig-node is currently a placeholder npm package.";
}

export default metadata;
